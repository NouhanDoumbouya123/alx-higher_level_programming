import unittest
from 0-add_integer import add_integer

class TestMyModule(unittest.TestCase):

    def test_add(self):
        """Test with numbers"""
        self.assertEqual(add_integer(1, 0), 1)
        self.assertEqual(add_integer(-1, 1), 0)
        self.assertEqual(add_integer(3, 3.5), 6.5)
